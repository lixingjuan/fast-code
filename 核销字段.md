```java
/**
 * 融资企业
 */
requestOrgId;
requestOrgName;

/**
 * 是否有可用回款 [Y/N/null]
 */
hasAvailableReceipt;

/**
 * 到期日
 */
Date dueDate;
dueDateStr;

/**
 * 计划还款截止日
 */
repaymentDateTo;

/**
 * 可用回款总额
 */
availableReceiptAmount = BigDecimal.ZERO;

/**
 * 本次核销金额
 */
writeOffAmount = BigDecimal.ZERO;

/**
 * 回款余额
 */
receiptBalance = BigDecimal.ZERO;

/**
 * 待核销数据
 */
List<WriteOffRequest> writeOffRequests;

/**
 * 用于核销的回款
 */
List<FundReceipt> receipts;


/**
     * 融资申请单id
     */
String requestId;
    /**
     * 融资申请单号
     */
String requestNumber;
    /**
     * 计息方式
     */
String interestChargeMethod;
    /**
     * 计划还款日期
     */
Date repaymentDate;

    /**
     * 融资申请单期数
     */
Integer periodTime;

    /**
     * 起息日
     */
Date loanDate;
    /**
     * 放款本金，融资金额
     */
loanAmount;


    /**
     * 剩余待还本金
     */
remainingLoanAmount = BigDecimal.ZERO;

    /**
     *  所有未还利息
     */
remainingInterestTotalAmount = BigDecimal.ZERO;

    /**
     *  所有未还费用
     */
remainingExpenseTotalAmount = BigDecimal.ZERO;

    /**
     * 本次还款本金
     */
writeOffLoanAmount = BigDecimal.ZERO;
    /**
     * 本次还款利息总额
     */
writeOffInterestTotalAmount = BigDecimal.ZERO;
    /**
     * 本次还款费用项总额
     */
writeOffExpenseTotalAmount = BigDecimal.ZERO;
```